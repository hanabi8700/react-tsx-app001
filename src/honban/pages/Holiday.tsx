// import React from 'react'
import { holidayList } from './Rokuyo';

function Holiday(betweenArray: Date[]): holidayList[] {
  const result = betweenArray.map((date) => {
    return {};
  });
  return result;
  
}

export default Holiday;
